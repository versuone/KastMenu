[req]
distinguished_name = req_dn
req_extensions = req_ext
prompt = no

[req_dn]
CN=kastmenu.{{HOSTNAME}}
OU=kastmenu
O={{HOSTNAME}}
DC=kastmenu.{{HOSTNAME}}

[ req_ext ]
basicConstraints       = CA:TRUE
subjectKeyIdentifier   = hash
authorityKeyIdentifier = keyid, issuer



